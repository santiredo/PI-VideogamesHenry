const initialState = {

}

export default function rootReducer(state = initialState, action) {

    switch(action.type){
        case '':
            // Código para el caso
            break;
            
        default:
            return state;
    }
}
